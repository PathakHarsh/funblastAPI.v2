# Production .env sample
# Copy this file to .env and replace placeholder values with real production values.
# WARNING: never commit real secrets to source control. Store in a secrets manager if possible.
# REQUIRED:
#   BASE_URL     - full API base URL (e.g. https://api.example.com)
#   TOKEN_KEY    - strong secret key (rotate regularly)
#   PORT         - port the app listens on (use 80/443 behind a reverse proxy)
# RECOMMENDED:
#   NODE_ENV=production
#   LOG_LEVEL=info
# Contact for full API access: droiddev04@gmail.com

BASE_URL=<apiBaseURL>
TOKEN_KEY=SoIzzPmA215A21fF51qLp
PORT=3000